[build]
  base = "web"
  command = "npm run build"
  publish = "web/dist"

[build.environment]
  # Substitua pela URL pública do seu backend
  VITE_SERVER_URL = "https://SEU_BACKEND_URL"

[functions]
  directory = "netlify/functions"

[dev]
  command = "npm run dev"
  port = 5173
  targetPort = 5173